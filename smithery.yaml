# Smithery.ai configuration
startCommand:
  type: stdio
  configSchema:
    type: object
    required:
      - ConnectionString
    properties:
      ConnectionString:
        type: string
        description: Connection string for the database
  commandFunction: |
    (config) => ({ 
      command: 'node', 
      args: ["server.js"],
      env: {}
    })